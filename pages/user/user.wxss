/*
用户管理页面 - 个人中心模块

上游依赖：user.wxml结构文件
入口：页面加载时自动应用样式
主要功能：定义用户界面的视觉样式，包括头像、统计卡片、菜单等组件
输出：渲染美观的用户界面样式

重要：每当所属的代码发生变化时，必须对相应的文档进行更新操作！
*/
/* pages/user/user.wxss */
.user-container {
  min-height: 100vh;
  background: linear-gradient(180deg, #f0f9ff 0%, #e0f2fe 100%);
}

/* v3 站内通知入口（右上角） */
.notify-entry{
  position: absolute;
  right: 30rpx;
  top: 30rpx;
  z-index: 20;
  width: 80rpx;
  height: 80rpx;
  border-radius: 40rpx;
  background: rgba(255,255,255,0.20);
  display:flex;
  align-items:center;
  justify-content:center;
  backdrop-filter: blur(6rpx);
}
.notify-icon{font-size:36rpx;color:#fff;}
.notify-dot{
  position:absolute;
  right:-6rpx;
  top:-6rpx;
  min-width: 34rpx;
  height: 34rpx;
  padding: 0 8rpx;
  border-radius: 18rpx;
  background:#ef4444;
  display:flex;
  align-items:center;
  justify-content:center;
}
.notify-text{color:#fff;font-size:20rpx;font-weight:700;}

/* 用户信息头部 */
.user-header {
  background: linear-gradient(135deg, #0ea5e9 0%, #06b6d4 100%);
  padding: 70rpx 40rpx;
  color: white;
  position: relative;
  overflow: hidden;
}

.user-header::before {
  content: '';
  position: absolute;
  top: -30%;
  right: -15%;
  width: 440rpx;
  height: 440rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
}

.user-info {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  margin-bottom: 50rpx;
  position: relative;
  z-index: 1;
}

.avatar-wrapper {
  padding: 0;
  margin: 0;
  background: transparent;
  border: none;
  width: 130rpx;
  height: 130rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 130rpx;
  flex-shrink: 0;
}

.avatar-wrapper::after {
  border: none;
}

.user-avatar {
  width: 130rpx;
  height: 130rpx;
  border-radius: 50%;
  background: white;
  color: #0ea5e9;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 60rpx;
  box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.15);
  overflow: hidden;
  flex-shrink: 0;
}

.user-avatar image {
  width: 100%;
  height: 100%;
}

.user-details {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  justify-content: center;
  margin-left: 16rpx;
  box-sizing: border-box;
  min-width: 0;
}

.user-name {
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.3;
  flex: 1;
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  display: block;
}

.user-name-container {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
}

.go-profile-icon {
  font-size: 36rpx;
  opacity: 0.7;
  flex-shrink: 0;
}

.user-uid {
  font-size: 22rpx;
  opacity: 0.8;
  line-height: 1.4;
}

.user-status {
  font-size: 24rpx;
  opacity: 0.9;
  line-height: 1.5;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  display: block;
}

/* 数据统计卡片 */
.user-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30rpx;
  background: white;
  padding: 36rpx;
  border-radius: 32rpx;
  position: relative;
  z-index: 1;
  box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.1);
}

/* 称号展示区域 (内联样式) */
.title-display-inline {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8rpx;
  width: 100%;
}

.title-label-inline {
  font-size: 22rpx;
  opacity: 0.8;
  font-weight: 500;
  white-space: nowrap;
}

.current-titles-inline {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  flex: 1;
  min-width: 0;
  gap: 8rpx;
}

.title-tag-inline {
  font-size: 20rpx;
  color: #0ea5e9;
  background: rgba(255, 255, 255, 0.3);
  padding: 4rpx 10rpx;
  border-radius: 16rpx;
  white-space: nowrap;
  backdrop-filter: blur(4rpx);
}

.achievement-tag-inline {
  font-size: 20rpx;
  color: #fbbf24;
  background: rgba(255, 255, 255, 0.3);
  padding: 4rpx 10rpx;
  border-radius: 16rpx;
  white-space: nowrap;
  backdrop-filter: blur(4rpx);
}

.no-title-inline {
  font-size: 20rpx;
  opacity: 0.7;
  font-style: italic;
}

.stat-item {
  text-align: center;
}

.stat-value {
  font-size: 44rpx;
  font-weight: 700;
  margin-bottom: 12rpx;
  background: linear-gradient(135deg, #0ea5e9 0%, #06b6d4 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.stat-label {
  font-size: 24rpx;
  color: #64748b;
}

/* 功能菜单 */
.user-menu {
  background: white;
  margin: 30rpx 40rpx;
  border-radius: 32rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
}

.menu-item {
  padding: 32rpx 40rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1rpx solid #f1f5f9;
  transition: background 0.2s;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-item:active {
  background: #f8fafc;
}

.menu-left {
  display: flex;
  align-items: center;
  flex: 1;
}

.menu-icon {
  width: 52rpx;
  margin-right: 28rpx;
  font-size: 44rpx;
}

.menu-label {
  font-size: 30rpx;
  color: #1e293b;
  font-weight: 500;
}

.menu-right {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.menu-badge {
  font-size: 24rpx;
  color: #ef4444;
  padding: 6rpx 16rpx;
  background: #fee2e2;
  border-radius: 20rpx;
}

.menu-badge.signed {
  color: #10b981;
  background: #d1fae5;
}

.menu-arrow {
  color: #cbd5e1;
  font-size: 36rpx;
}
